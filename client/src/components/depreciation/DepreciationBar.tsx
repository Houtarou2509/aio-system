/**
 * DepreciationBar — deprecated.
 * Depreciation fields have been removed from the Asset model.
 * This component is kept as a stub to avoid breaking imports.
 */
import { Asset } from '../../lib/api';

export function DepreciationBar({ asset: _asset }: { asset: Asset }) {
  return null;
}